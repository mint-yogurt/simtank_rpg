# Auto-synced from hub_fronthouse.json by populate_yamls.py.
# id/name/type are overwritten on every sync; missing stub fields
# (dialogue, contents, event, sprite, behavior, destination_map,
# destination_warp, facing, distance) are seeded per type but never
# overwritten once filled in -- see STUB_FIELDS in the script.

{}
